<div>
    <div class="form-group">
        <label for="material-value">{{localize "pf2e-monster-parts.dialog.monster-part-editor.value"}}</label>
        <input type="number" name="material-value" id="material-value" value="{{value}}" step="0.01" />
        <p style="flex-grow: 0;">{{localize currencyLabel}}</p>
    </div>
    <table>
        <tr>
            <td>
                <fieldset>
                    <legend>{{localize "pf2e-monster-parts.dialog.monster-part-editor.refinements"}}</legend>
                    <div style="overflow-y:scroll;height:400px;">
                        {{#each refinements}}
                            <p>
                                <input type="checkbox" name="{{this.key}}" id="{{this.key}}" {{checked this.checked}}/>
                                <label for="{{this.key}}">{{this.label}}</label>
                            </p>
                        {{/each}}</div>
                </fieldset>
            </td>
            <td>
                <fieldset>
                    <legend>{{localize "pf2e-monster-parts.dialog.monster-part-editor.imbuements"}}</legend>
                    <div style="overflow-y:scroll;height:400px;">
                        {{#each imbuements}}
                            <div class="form-group">
                                <input type="checkbox" name="{{this.key}}" id="{{this.key}}" {{checked
                                    this.checked}}/>
                                <a onclick="game.modules.get('pf2e-monster-parts').api.renderSummaryJournal('{{this.key}}')">{{this.label}}</a></div>
                        {{/each}}
                    </div>
                </fieldset>
            </td>
        </tr>
    </table>
</div>